'use strict';

/**
 * titulo-tabla-liga service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::titulo-tabla-liga.titulo-tabla-liga');
