'use strict';

/**
 * proximo-partido controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::proximo-partido.proximo-partido');
